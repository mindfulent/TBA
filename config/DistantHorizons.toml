# Distant Horizons config - TBA defaults
# Disable startup warnings since players get our welcome screen
_version = 3

[common]
	[common.logging]
		[common.logging.warning]
			showLowMemoryWarningOnStartup = false
			showPoolInsufficientMemoryWarning = false
			showHighVanillaRenderDistanceWarning = false
			showReplayWarningOnStartup = false
			showUpdateQueueOverloadedChatWarning = false
			showSlowWorldGenSettingWarnings = false
			showModCompatibilityWarningsOnStartup = false

[client]
	[client.advanced]
		# Suppress the "New update available!" prompt on first launch.
		# DH is pinned to 2.4.5-b deliberately because 3.0.x breaks server-side
		# mixins (Travelers Backpack LanguageMixin crash, see CHANGELOG v1.0.3
		# for the incident). Players should never click "Update" — so don't ask.
		[client.advanced.autoUpdater]
			enableAutoUpdater = false
			enableSilentUpdates = false
